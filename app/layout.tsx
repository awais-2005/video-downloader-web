import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Video Downloader - Download Videos from Any Platform",
  description:
    "A simple and fast video downloader that supports multiple formats and qualities. Download videos from YouTube and other platforms.",
  keywords: "video downloader, youtube downloader, video converter, download videos",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
